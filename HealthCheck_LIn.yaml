---
- name: List running services and disk usage
  hosts: "{{ selected_host }}"
  become: yes
  vars:
    output_file: "running_services_{{ ansible_hostname }}.txt"

  tasks:
    - name: Get running services
      shell: systemctl list-units --type=service --state=running
      register: running_services

    - name: Show running services in stdout
      debug:
        msg: "{{ running_services.stdout_lines }}"

    - name: Get disk space usage
      shell: df -h
      register: disk_usage

    - name: Show disk space usage in stdout
      debug:
        msg: "{{ disk_usage.stdout_lines }}"
