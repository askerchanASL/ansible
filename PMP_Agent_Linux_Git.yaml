- name: Install UEMS Linux Agent
  hosts: all
  vars:
    repo_url: "https://github.com/askerchanASL/ansible.git"
    repo_dest: "/tmp/ME"
    #ansible_become_pass: "P@ssw0rd"
    #ansible_become_password: p@ssw0rd
      
  tasks:
    - name: Clone UEMS Linux Agent from Git
      ansible.builtin.git:
        repo: "{{ repo_url }}"
        dest: "{{ repo_dest }}"
        version: "main"
        force: yes

    - name: Make UEMS_LinuxAgent.bin executable
      ansible.builtin.file:
        path: "{{ repo_dest }}/LinuxAgent/UEMS_LinuxAgent.bin"
        mode: '0755'

    - name: Whoami
      ansible.builtin.command:
        cmd: whoami

    - name: Run the installer
      command: "sudo {{ repo_dest }}/LinuxAgent/UEMS_LinuxAgent.bin"
      become: true
      
    - name: Remove Folder
      ansible.builtin.file:
        path: "{{ repo_dest }}"
        state: absent
